#include "events.h"



/*void events::Events::addObserver(const UnitIdentifier& objectId, ObserverPtr& obs)
{
	m_observers.insert({ objectId, obs });
}

void events::Events::removeObserver(const UnitIdentifier& objectId)
{
	m_observers.erase(objectId);
}

// TODO try catch maybe
void events::Events::notifyObservers(const UnitIdentifier& objectId, const std::string& eventName) const
{
	for (const auto [id, obs] : m_observers) {
		if (id == objectId)
			obs->newUnitSelected(objectId, eventName);
	}
}*/
 

